---
layout: layouts/base.njk
permalink: /
---

<section class='about container section-margin'>
  <h1 id='about'>Hi, I'm David.</h1>
  <p class='about__text'>
    I'm a informatic engineer living in Caracas, VE and I enjoy building stuffs  whether that be websites, applications, or anything in between with a <span class='highlight--primary'>main focus on frontend development.</span>
  </p>
</section>
<section class='work container section-margin'>
  <h2 id='work'>Some things I've built</h2>
  <p class='about__text'>I have experience working in a wide range of technologies including <span class='highlight--primary'>React, Redux, Vue, Sass, CSS, Jest, PostgreSQL, Git, Gatsby and many others.</span>
  </p>
  {%- if collections.projects.length == 0 -%}
    <p class='highlight--secondary'>Coming soon...</p>
  {%- else -%}
    {% for project in collections.projects %}
      <div class='work__project-container'>
        <a href="{{ project.url | url }}">
          <h3 class="highlight--secondary work__title">{{ project.data.title }}</h3>
        </a>
        <img class="work__hero" src="{{ project.data.image }}" alt="{{ project.data.title }}" height="500" width="1000"/>
        <ul class='work__tags-list'>
          {% for tag in project.data.tags | filterTagList %}
            <li class='work__tags-element'>
              <p>{{ tag }}</p>
            </li>
          {% endfor %}
        </ul>
        <p>{{project.data.description}}</p>
        <div class='work__resources'>
          <a href="{{ project.url | url }}" class="button button--link work__button">View case study</a>
          <div class='work__external-resources'>
            <a href="{{project.data.repository_url}}">Repository {%- include "external-link.njk" -%}
            </a>
            <a href="{{project.data.live_url}}">Live demo {%- include "external-link.njk" -%}
            </a>
          </div>
        </div>
      </div>
    {% endfor %}
  {%- endif -%}

</section>

<section class='contact container section-margin'>
  <h2 id='contact'>Contact</h2>
  <p>If you have something in mind and want me to work with you or your team, leave a message  and I'll get back to you!</p>

  <form action="/api/contact" method="POST" class='contact__form'>
    <label class='contact__label highlight--secondary' for="email">Email</label>
    <input class='contact__input' id="email" name="email" type="email" required/>
    <label class='contact__label highlight--secondary' for="message">Message</label>
    <textarea class='contact__input' id="message" name="message" rows="6" type="text" required autocomplete='false'></textarea>
    <button type="submit" class='contact__submit button'>Send Message</button>
  </form>
</section>